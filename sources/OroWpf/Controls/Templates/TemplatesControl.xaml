<UserControl 
    x:Class="DustInTheWind.ClockWpf.ClearClock.Controls.Templates.TemplatesControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:templates="clr-namespace:DustInTheWind.ClockWpf.ClearClock.Controls.Templates"
    xmlns:clockwpf="clr-namespace:DustInTheWind.ClockWpf;assembly=DustInTheWind.ClockWpf"
    
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:DataContext="{d:DesignInstance templates:TemplatesViewModel}">

    <DockPanel>

        <ComboBox
            DockPanel.Dock="Top"
            DisplayMemberPath="Name"
            Margin="0 5 0 0"
            ItemsSource="{Binding TemplateTypes}"
            SelectedItem="{Binding SelectedTemplateType}"/>

        <clockwpf:AnalogClock
            x:Name="analogClock1"
            ClockTemplate="{Binding SelectedTemplate}"
            Margin="0 10 0 0"/>

    </DockPanel>

</UserControl>
